'use strict';

/**
 * directory-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::directory-category.directory-category');
